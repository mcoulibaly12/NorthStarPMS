import type { Meta, StoryObj } from '@storybook/react'

import FinalizePage from './FinalizePage'

const meta: Meta<typeof FinalizePage> = {
  component: FinalizePage,
}

export default meta

type Story = StoryObj<typeof FinalizePage>

export const Primary: Story = {}
